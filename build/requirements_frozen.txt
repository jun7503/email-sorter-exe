# --- scientific stack (unchanged) ---
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
pandas==2.2.3

# Excel I/O
openpyxl==3.1.5
xlrd==2.0.1

# --- .msg parsing ---
# pip package name uses hyphen; import name is extract_msg (underscore)
extract-msg>=0.52.0
olefile>=0.47
# RTF helpers actually used with extract-msg
rtfde>=0.1.0
compressed-rtf>=1.0.6
# Optional charset detection (good to keep)
chardet>=5.0.0
# Optional: Windows time zone DB
tzdata>=2024.1; platform_system == "Windows"
